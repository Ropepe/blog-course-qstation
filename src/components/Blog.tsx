import * as React from "react";

interface IBlogProps {}

const Blog: React.FunctionComponent<IBlogProps> = (props) => {
  return (
    <>
      <div>Perica</div>
    </>
  );
};

export default Blog;
